# Used for voice-to-text transcription.
ELEVENLABS_API_KEY=

# Used for OpenRouter API access (reaches the jev model).
OPENROUTER_API_KEY=

# Persist both secrets to the OS keychain with: bun cterm login
